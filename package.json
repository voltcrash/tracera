{
  "name": "tracera",
  "private": true,
  "scripts": {
    "build": "vp run -r build",
    "build:vercel": "vp run --filter api --filter web build && node scripts/build-vercel.mjs",
    "dev": "vp run --parallel --filter api --filter web dev",
    "check-types": "vp run -r check-types",
    "prepare": "vp config"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12"
  },
  "packageManager": "pnpm@11.20.0"
}
