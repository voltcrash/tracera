DATABASE_URL=postgresql://localhost:5432/tracera
REDIS_URL=redis://127.0.0.1:6379
PORT=3001
# Origin allowed to send credentialed browser requests (the Next.js app in local development).
WEB_ORIGIN=http://localhost:3000

# Local AI setup (requires Ollama and the models below).
AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=gemma2:9b
# Bound local-model output so a malformed structured response cannot hold a
# request forever. Increase only after validating the model's latency.
OLLAMA_MAX_TOKENS=512
# Must be 1024-dimensional to match the pgvector schema.
OLLAMA_EMBEDDING_MODEL=mxbai-embed-large

# Optional retrieval providers. Leave blank to rely on the claim corpus + GDELT.
# GOOGLE_FACT_CHECK_API_KEY=
# NEWS_API_KEY=
# WEB_SEARCH_ENDPOINT=
# WEB_SEARCH_API_KEY=

# Retrieval tuning.
CORPUS_SIMILARITY_THRESHOLD=0.78
DEDUP_SIMILARITY_THRESHOLD=0.92
DEDUP_MAX_AGE_HOURS=24

# Required by the decay worker when it asks this API to bypass deduplication
# and create the next linked version of a check.
INTERNAL_WORKER_TOKEN=replace-with-a-long-random-secret

# Optional delivery for material score changes made by the decay worker.
# If omitted, changes are still persisted in the Trace Timeline.
# RESEND_API_KEY=
# ALERT_FROM_EMAIL=Tracera <alerts@example.com>
ALERT_SCORE_DELTA=5
PUBLIC_WEB_URL=http://localhost:3000
