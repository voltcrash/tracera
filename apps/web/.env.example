# Optional origin override for local development, where the Next.js dev server
# and the Hono server run on different ports. Leave unset in production so the
# browser talks to the same Vercel origin that serves the static app.
# NEXT_PUBLIC_SERVER_ORIGIN=http://localhost:8787
NEXT_PUBLIC_SITE_URL=https://tracera.voltcrash.com
