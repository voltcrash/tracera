packages:
  - "apps/*"
  - "packages/*"
minimumReleaseAge: 0
allowBuilds:
  esbuild: true
  msgpackr-extract: true
  sharp: true
  spawn-sync: true
  # Wrangler's local preview runtime is distributed as a package build.
  workerd: true
