# Task 12 handoff

Task status: **BLOCKED**

Implementation status: **PASS**. The Task 12 release evaluator, evaluation report, operational
runbook, release decision, and explicit production configuration proposal are complete. The
required deterministic fixtures pass.

Release-validation status: **BLOCKED**. The required human-gold corpus, development model
comparison, adjudicated calibration artifact, sealed-test inputs, authorized paid/live batch,
independent human audit, staging environment, production worker host, and approved routing switch
are unavailable. No production migration, deployment, shadow traffic, canary, or paid provider
request was run.

- Parent branch: `core-v2/task-11-unify-orchestration`
- Parent commit used: `daef5bad814941280e37fb5d99732c749f727af3`
- Required configured base commit: `6f6e28f0e384100986fcc9ed61bea6fe34233a48`
- Task branch: `core-v2/task-12-release-evaluation`
- Stack position: Core v2 task 12 of 13
- PR base: `core-v2/task-11-unify-orchestration`
- Predecessor PR: #17, `https://github.com/voltcrash/tracera/pull/17`

The task configuration named `6f6e28f` as the predecessor base, but the verified predecessor branch
and PR #17 had advanced to `daef5ba`. This task was branched from that actual pushed tip. The
predecessor was not amended, rebased, force-pushed, merged, or retargeted.

## Changed files and exact behavior

Task 12 evaluation artifacts:

- `packages/ai/evaluation/release-fixtures.json` defines the frozen eight-case synthetic release
  manifest and explicitly lists the missing live evidence prerequisites.
- `packages/ai/scripts/support/release-scenarios.ts` exercises the frozen Core v2 seams for
  retrieval prompt injection, SSRF, source flooding, corpus poisoning, number/unit changes, future
  leakage and split contamination, missing/uncertain OCR, and altered-image reuse.
- `packages/ai/scripts/evaluate-core-release.ts` runs only the `fixture` mode for `split=all` and
  seed `20260910`. It emits per-case checks, a manifest hash, and explicit `not_evaluated` values
  for empirical metrics, actual cost, p50, and p95. Live and replay modes exit 1 with a
  `not_evaluated` prerequisite message.
- `packages/ai/test/core-release-evaluation.test.ts` runs one test for each required adversarial
  fixture case.

Task 12 documentation:

- `docs/core-overhaul/EVALUATION-REPORT.md` records split isolation, candidate/model status,
  calibration policy, PLAN gate ledger, fixture/replay/live distinctions, hashes, and operational
  evidence.
- `docs/core-overhaul/OPERATIONS-RUNBOOK.md` records safe local setup, migration and worker
  procedures, staging rehearsal acceptance, backpressure/spend/deletion/routing recovery, gated
  evaluation steps, and incident handling.
- `docs/core-overhaul/PRODUCTION-CONFIGURATION-PROPOSAL.md` proposes Vercel for the existing web
  plane and a separate Fly Machines long-running `worker` process group, role-separated secrets,
  exact starting limits, migration/deployment commands, canary scope, and recovery rules. It is a
  proposal only; no host or deployment file was selected or created.
- `docs/core-overhaul/RELEASE-DECISION.md` records BLOCKED per gate, proposed limits, unknown cost
  and latency, artifacts, approval-gated commands, and reopen conditions.
- `docs/core-overhaul/handoffs/12.md` is this handoff. Predecessor handoffs were not changed.

Shared-file changes:

- `package.json` adds `evaluate:core-release:fixture`, the root generated-profile wrapper for the
  Task 12 evaluator.
- `packages/ai/package.json` adds the guarded `evaluate:core-release` entry point and the focused
  `test:core-release` script. These are necessary to run the evaluator/test through Vite+ and the
  existing environment guard. No dependency, lockfile, contract, stage signature, migration, v1
  route, or production runtime behavior changed.

## Contract and API examples

The frozen Core v2 contracts and stage boundaries remain unchanged. The bounded evaluator is
invoked through the existing Vite+ environment boundary:

```sh
vp run evaluate:core-release:fixture
```

The evaluator output has this shape:

```json
{
  "mode": "fixture",
  "split": "all",
  "seed": 20260910,
  "passed": 8,
  "failed": 0,
  "skipped": 0,
  "checks": 18,
  "humanGoldClaims": 0,
  "empiricalMetrics": {
    "status": "not_evaluated",
    "numerator": null,
    "denominator": 0,
    "confidenceInterval": null
  },
  "releaseGateStatus": "blocked",
  "releaseApproved": false
}
```

Production activation remains the Task 13 boundary. The proposed worker command is:

```sh
vp run @repo/ai#worker:core
```

It must run with the deployed runtime role, a non-fixture provider, the frozen `RunEnvironment`,
durable lease/fencing, owner-scoped repository, cancellation, retry, and provider reservation
controls from Task 11.

## Security, tenant, owner, visibility, cancellation, and audit behavior

The new fixture cases assert that:

- document instructions remain untrusted data in the structured evidence prompt;
- private and rebound SSRF targets are rejected before the redirected request;
- flooding is bounded by the one shared run budget and reports `budget_exhausted`;
- corpus proposals are revalidated by content hash, tenant, owner, visibility, proposition scope,
  temporal scope, retriever version, and complete snapshot role; prior model output is never
  evidence;
- altered denominators/units, future evidence, invalid OCR, and changed image content are not
  silently normalized into verified or reusable output;
- exact report reuse remains owner/tenant/visibility scoped and requires exact content,
  proposition, version, and freshness identity. Similar stories or images remain context only.

Task 11's durable worker continues to fence stale attempts, distinguish browser disconnect from
explicit cancellation, avoid canceled scorecards, settle reservations, and record scoped audit
events without copying untrusted document text. No Task 12 change weakens authentication,
publication consent, tenant/owner isolation, spend controls, or legacy v1 rendering.

## Configuration and migration

No configuration, secret, credential, or migration file was changed. The generated local/test
configuration is worktree-specific and ignored. `vp run env:setup` generated:

- worktree ID `t3code_db55ae76_d957bd00`;
- web origin `http://localhost:45269`;
- local PostgreSQL `127.0.0.1:23773`;
- disposable test PostgreSQL `127.0.0.1:23774`.

The proposal uses the existing deployed runtime/migration split. Production migration is an
approval-gated additive `vp run @repo/db#db:migrate` under `TRACERA_CONFIG_ROLE=migration`; it was
not run. The runtime/worker profile must set an exact approved stable `AI_MODEL` before activation;
the repository example currently leaves it unset. The deterministic fixture provider and synthetic
calibrator remain unavailable in deployed profiles.

Current proposed starting limits are 120 external requests/run, 12 discovery queries/claim, 20
fetched candidates/claim, 3 provenance hops, 2 targeted rounds, 600,000 ms elapsed, 3 concurrent
external calls, 30 user requests/hour, 60 IP requests/hour, 2 user/4 IP concurrent runs, 100
user/day, a 3,600-second forced-reanalysis cooldown, a 600-second admission lease, 86,400-second
idempotency retention, daily provider spend USD 25, and one initial worker replica. A numeric
per-run cost ceiling remains unresolved until authorized provider cost data exists; production paid
traffic must remain disabled.

## Commands and exact outcomes

Environment: macOS, Node `v24.21.0`, Vite+ `v0.3.1` with test runner `v4.1.11`, PostgreSQL 18
with pgvector through OrbStack, and Chromium `153.0.8010.12`. No production credential, external
provider, paid request, production database, or deployment was used.

Predecessor/prerequisite validation:

- `vp install`: PASS; 8 workspace projects already current, pnpm `11.20.0` through Vite+.
- `vp run env:setup`: PASS; isolated generated configuration as listed above.
- `vp run test:core-storage`: PASS; 1 file, **5 passed, 0 failed, 0 skipped**. Disposable run
  database dropped.
- `vp run db:test:status`: expected exit 1 after cleanup; test database/container reported `not
created`.
- `vp run db:rehearse`: PASS; 29 journal migrations applied with hashes/timestamps, repeated
  migration was a no-op, 31-table runtime privilege map matched, and the runtime PostgreSQL suite
  was 1 file, **6 passed, 0 failed, 0 skipped**. The generated run database was dropped.
- `vp run test:integration`: PASS; local migration/rehearsal, Core storage, browser auth, offline
  analysis, and Core fixture workflow completed. The separately provisioned suites were Core
  storage **5/0/0**, PostgreSQL runtime **6/0/0**, auth browser **1/0/0**, and offline browser
  **1/0/0**, all with zero skipped tests. The pre-existing local database was left running by the
  workflow; the test container remained not created after cleanup.

Task-specific validation:

- `vp run @repo/ai#test:core-release`: PASS; 1 file, **8 passed, 0 failed, 0 skipped**.
- `vp run evaluate:core-release:fixture`: PASS; **8 cases passed, 0 failed, 0 skipped, 18
  checks**. Fixture manifest bytes hash:
  `sha256:4aff624c8b3e3bfa0b581073d4c186f2753b4eb0aea67b9147119e0a3defe4c7`.
- The eight fixture cases each pass; the missing-OCR case has 4 checks and the other seven have 2
  checks each. Empirical metrics are `not_evaluated`, numerator `null`, denominator `0`, and
  confidence interval `null`; actual cost, p50, and p95 are `null`.
- `evaluate:core-release` with `--mode replay`: expected exit 1 with
  `status=not_evaluated`; live/replay release evaluation is authorization- and human-gold-gated.
- The exact unprofiled required command
  `vp run @repo/ai#evaluate:core --mode fixture --split all --seed 20260910`: expected exit 1
  because the environment guard rejects the absent `TRACERA_PROFILE`. The authoritative wrapped
  `vp run evaluate:core:fixture` passed the Task 01 invariant fixture: 4/4 detectors, 5 synthetic
  claims, 0 human-gold claims, canonical dataset hash
  `sha256:7b4caa72a9b0fa33c1a45d0dd1782aa30d78d2fb48884c61d5890732ea63daae`, and all empirical
  metrics `not_evaluated`.

Predecessor regression validation:

| Command family              | File/cases                          |      Passed | Failed | Skipped |
| --------------------------- | ----------------------------------- | ----------: | -----: | ------: |
| Core ingestion              | 1 file                              |           9 |      0 |       0 |
| Core claims                 | 1 file                              |          14 |      0 |       0 |
| Core retrieval              | 1 file                              |          13 |      0 |       0 |
| Core evidence               | 1 file                              |          12 |      0 |       0 |
| Core provenance             | 1 file                              |          11 |      0 |       0 |
| Core adjudication           | 1 file                              |          17 |      0 |       0 |
| Core scoring                | 1 file                              |          14 |      0 |       0 |
| Core orchestration          | 1 file                              |          14 |      0 |       0 |
| Core evaluator: invariants  | 5 synthetic claims                  | 4 detectors |      0 |       0 |
| Claims evaluator            | 7 checks; 13/13 synthetic alignment |           7 |      0 |       0 |
| Retrieval evaluator fixture | 5 cases                             |           5 |      0 |       0 |
| Retrieval evaluator replay  | 5 cases                             |           5 |      0 |       0 |
| Evidence evaluator          | 7 cases                             |           7 |      0 |       0 |
| Provenance evaluator        | 7 cases                             |           7 |      0 |       0 |
| Adjudication evaluator      | 7 cases                             |           7 |      0 |       0 |
| Scoring evaluator           | 8 cases                             |           8 |      0 |       0 |
| Orchestration evaluator     | 12 cases; 61 checks                 |          12 |      0 |       0 |

Repository-wide validation:

- `vp check`: PASS; all 322 checked files formatted and 242 files had no warnings, lint errors,
  or type errors.
- `vp test --run`: **30 files passed, 2 skipped; 211 tests passed, 0 failed, 11 skipped**. The
  11 skips are the 5 Core storage and 6 PostgreSQL rehearsal tests intentionally skipped by the
  plain runner because they require separate provisioning. Their authoritative commands above
  ran all 11 with zero skips.
- `vp run check-types`: PASS; 7 workspace tasks.
- `vp run build`: PASS; local-profile production-mode Next build with 13 routes, including
  `/run/[id]`.
- `git diff --check`: PASS.

Warnings printed by local Playwright web servers (`NO_COLOR`/`FORCE_COLOR`) and expected fixture
provider-failure logs did not fail any suite. No failure caused by Task 12 remains unresolved.

## Evaluation hashes, metrics, and distinctions

| Artifact/evaluator                    | Hash or result                                                                                         |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Core invariant canonical dataset      | `sha256:7b4caa72a9b0fa33c1a45d0dd1782aa30d78d2fb48884c61d5890732ea63daae`                              |
| Core invariant dataset file bytes     | `sha256:07637959336fbb46ae8e871de15484699832a27609173a1e4f0e74a4df1fffdf`                              |
| Candidate dataset manifest file bytes | `sha256:be1a138d48ddc363d17a0d9e6c1cd0dbc5ef1a5c5495eef2d95f958024b26d09`                              |
| Annotation queue file bytes           | `sha256:1c04a0ea75d4a4406723496d5dc487eb2278961dfffd9846196c521c234280f0`                              |
| Task 12 release fixture manifest      | `sha256:4aff624c8b3e3bfa0b581073d4c186f2753b4eb0aea67b9147119e0a3defe4c7`; 8/8 cases, 18/18 checks     |
| Task 11 orchestration fixture         | `sha256:6f9b86ae71343b36ff7f7f1d6b52eb9a575364ef582f8c708061b4f40a202979`; 12/12, 61/61                |
| Task 06 retrieval replay fixture      | `sha256:188ccd0ab26855340288895485b0101d5bf19e5420c6e7b16aff155c734bcf02`; 5/5 fixture and 5/5 replay  |
| Claims fixture evaluator              | `sha256:a963e29cb0a51198cdfe497656bc8bf13c923c9c203bd94409cfc5d1d22b97c5`; 7/7 checks, 13/13 alignment |
| Evidence fixture evaluator            | `sha256:53d602c237b595a27c1a158d674b7974895f72060f07286095f07ba3043dca5a`; 7/7                         |
| Provenance fixture evaluator          | `sha256:7a8739348b008f0d14b0e3f4da66934b2621f62f4d93979721d558ba6ddfb505`; 7/7                         |
| Adjudication fixture evaluator        | `sha256:fceaf4895b33daf95e922eb91429a06beb2ba15f7c96398f8124320f67f6484d`; 7/7                         |
| Scoring fixture evaluator             | `sha256:603024a5c1e4b44a2319397ceda6efd61d4f4b7dae57f33dae732819233a44c8`; 8/8                         |

Fixture evidence uses scripted providers, deterministic content, and in-memory repositories. It
demonstrates invariants and expected failures only. Replay evidence recomputes from persisted
immutable inputs and does not represent a stochastic provider rerun. PostgreSQL evidence is a
disposable real database with synthetic rows and is not production or staging evidence.

Human gold is absent: 0/1,500 target claims, 0/500 development, 0/500 calibration, 0/500 sealed
test, and no temporal holdout. Consequently claim, citation, evidence, retrieval, decisive
precision, coverage, macro-F1, calibration, origin, and improvement metrics all have numerator
`null`, denominator `0`, and status `not_evaluated`; no confidence interval is reported. No
provider/model accuracy, cost, latency, or behavior was measured.

## Unresolved blockers and remaining release gates

1. Collect and license-review at least 1,500 independently adjudicated claims from at least 300
   event/source-isolated stories, with required categories/languages and a later temporal holdout.
2. Compare exact latest stable configured provider/model candidates on development data only and
   record capability, cost, latency, outage, budget, prompt, engine, and retriever artifacts.
3. Fit the frozen out-of-fold calibrator from the calibration partition, freeze thresholds and
   hashes, and open the sealed test once without tuning on test failures. The current policy needs
   500 adjudicated decisive observations, 50 per advertised slice, 5 grouped folds, L2=1, and a
   0.95 precision target.
4. Run authorized fixed-evidence, retrieval replay, matched live v1/v2, and adversarial batches;
   independently audit unsupported/confident errors; report all PLAN numerators, denominators,
   Wilson bounds, clustered-bootstrap seed, outages, omissions, and unknowns.
5. Provide a real staging host and rehearse additive migration, worker failure/retry, backpressure,
   deletion, spend exhaustion, routing recovery, and host limits.
6. Select and authorize the proposed worker host, exact pinned model IDs, conservative per-run cost
   ceiling, role-separated secrets, auditable routing switch, canary cohort, and recovery command.

The current safe production limits are zero enabled v2 traffic and zero paid provider runs. The
numeric starting limits in the proposal are not approved until the above gates pass. V2 remains
disabled outside local/test fixture profiles; v1 behavior and historical reports remain intact.

## Successor instructions

`NEXT_TASK_NUMBER` is `-`; no successor branch may be created from this task. If the release work
is resumed, it must start from this task's final pushed tip, preserve this BLOCKED decision until
the missing inputs are genuinely supplied, and never treat fixture, replay, PostgreSQL, browser, or
operational evidence as empirical accuracy. A future cutover task must obtain explicit authorization
for the concrete proposal before touching production migration, deployment, routing, or legacy
retirement.

Parent commit: `daef5bad814941280e37fb5d99732c749f727af3`.
