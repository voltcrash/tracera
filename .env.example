# Root .env files are intentionally not loaded by Tracera.
#
# Local and test configuration is generated per worktree:
#   vp run env:setup
#
# Deployment examples are split by process role under config/environment/.
# Do not combine those files or copy production values into this repository.
